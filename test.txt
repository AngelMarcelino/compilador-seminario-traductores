void main () {
  int x = 0;
  float y = 1;
  int z = 3;
  char q = 3;
  if ( x < 3 && x > 4 && x <= 4 || x >= 5 || x == 4 && x != 45) {
    doSomething(x, y, z);
    while (true) {
      print(hola);
    }
  } else {
    y = x + 1;
    x = x + 2 * 4 / 43 - 5;
  }
  return 0;
}

void main () {
  int x;
  float y;
  int z;
  char q;
  x = 0;
  y = 1;
  z = 3;
  q = 3;
  if ( x < 3 && x > 4 && x <= 4 || x >= 5 || x == 4 && x != 45) {
    doSomething(x, y, z);
    while (true) {
      print(hola);
    }
  } else {
    y = x + 1;
    x = x + 2 * 4 / 43 - 5;
  }
  return 0;
}


int main(int x, int y, int z) {
  a=1;
  a = 2;
}
